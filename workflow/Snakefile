import glob


configfile: "config/config.yml"

# Access the data folder from the config
data_folder = config["data"]["folder"]

# Glob for sample files in the specified data folder
sample_files = glob.glob(f"{data_folder}/*.bam")

rule all:
    input:
        sample_files
